# Pinned environment for GPU rendering of the velocity-intervention dataset.
#
# Python MUST be 3.11: the `bpy==4.2.0` wheel (Blender 4.2 LTS, Cycles with
# CUDA/OptiX kernels) only ships cp311 builds. This is the same interpreter /
# Blender pairing described in docs/environment_thesis.md.
bpy==4.2.0
numpy>=1.26,<3
pybullet==3.2.7
traitlets>=5.9,<6
pyquaternion>=0.9.9
etils[epath]>=1.5
imageio>=2.31
imageio-ffmpeg>=0.4.9
OpenEXR>=3.2
Pillow>=10
bidict>=0.22
PyYAML>=6
scipy>=1.11
trimesh>=4
munch>=4
pypng>=0.20
tensorflow-cpu>=2.16,<2.22
pandas>=2
scikit-learn>=1.3
huggingface_hub>=0.24
pytest>=7
hypothesis>=6
